# Core — required for the engine to run end-to-end
pydantic>=2.6
python-dotenv>=1.0
numpy>=1.26

# LLM auditor (Anthropic Claude). Without ANTHROPIC_API_KEY the engine falls
# back to a deterministic heuristic auditor so the pipeline still runs.
anthropic>=0.49

# Supabase (Postgres + pgvector) backend. Without SUPABASE_URL/SUPABASE_KEY the
# engine falls back to a local SQLite + numpy store with the same interface.
supabase>=2.4

# --- Optional ---
# Better embeddings (local, no API key). If absent, a deterministic hashing
# embedder is used so retrieval still works offline.
#   pip install sentence-transformers
#
# Thin demo UI:
#   pip install streamlit
